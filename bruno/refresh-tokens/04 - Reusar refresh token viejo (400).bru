meta {
  name: 04 - Reusar refresh token viejo (400)
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/auth/refresh
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "refreshToken": "{{oldRefreshToken}}"
  }
}
